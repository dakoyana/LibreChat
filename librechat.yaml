version: 1.0.0
cache: true

# SCL Agents Hub - Minimal Configuration
interface:
  customWelcome: |
    👋 Welcome to **SCL Agents Hub**, {{user.name}}!
    
    Context-aware SCL agents. They are SCL team members, specialised to help push our projects forward. We develop new agents and kill the ones that suck.

endpoints:
  # Enable agents - the core of SCL Agents Hub
  agents:
    disableBuilder: false
    capabilities: ["execute_code", "file_search", "actions", "tools", "artifacts", "ocr", "chain", "web_search"]
